{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install --no-cache-dir -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd /app && uvicorn main:app --host 0.0.0.0 --port 8080"
  }
}
